import { WorkInProgress } from "@/components/WorkInProgress";

// Diligence Tracker — announced on the landing page, not built yet.
export default function DiligencePage() {
  return <WorkInProgress title="Diligence Tracker" />;
}
